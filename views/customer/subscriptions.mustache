{{> header}}
<div class="container">
{{> customerNav }}
<div class="content">
<h1>MySaas</h1>

<div>
    <h2>Subscriptions</h2>
    {{ #plans }}
    <div id="{{ id }}">
        <h3>{{ name }}</h3>
        <span>{{ price }}</span>
        <p>{{ description }}</p>
    </div>
    {{ /plans }}
<div>
</div>
</div>

{{> footer}}